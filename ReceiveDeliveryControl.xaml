<UserControl x:Class="PVZManagerWPF.ReceiveDeliveryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Поставка:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="cboDelivery" Width="200" DisplayMemberPath="waybill_number" SelectedValuePath="delivery_id"/>
            <Button x:Name="btnAccept" Content="Принять заказы" Width="120" Margin="10,0,0,0" Click="BtnAccept_Click"/>
        </StackPanel>
        <DataGrid x:Name="dgvOrders" Grid.Row="2" AutoGenerateColumns="True" IsReadOnly="True"/>
    </Grid>
</UserControl>